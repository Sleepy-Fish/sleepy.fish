import {
  BLUE,
  GREEN,
  RED,
  GRAY,
  WHITE,
  BLACK,
  GRADIENT,
  SHADOW,
  STROKE,
  LG,
  MD,
  SM,
  HEADER_HEIGHT,
  SPLASH_HEIGHT,
  NAV_HEIGHT,
  SM_NAV_HEIGHT,
} from './style';

import { ROUTES, LINKS } from './data';

export const COLOR = {
  BLUE,
  GREEN,
  RED,
  GRAY,
  WHITE,
  BLACK,
};

export const STYLE = {
  GRADIENT,
  SHADOW,
  STROKE,
};

export const BREAKPOINT = {
  LG,
  MD,
  SM,
};

export const APP = {
  HEADER_HEIGHT,
  SPLASH_HEIGHT,
  NAV_HEIGHT,
  SM_NAV_HEIGHT,
};

export const DATA = {
  ROUTES,
  LINKS,
};
